import React, { FC } from "react";
import Editor from "../../components/Editor/Editor";
interface props {}

const Layout = ({}) => {
  return (
    <main>
      <Editor />
    </main>
  );
};

export default Layout;
